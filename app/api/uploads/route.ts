import { env } from 'cloudflare:workers';
export async function POST(request:Request){try{const form=await request.formData();const file=form.get('file');if(!(file instanceof File)||!file.type.startsWith('image/'))return Response.json({error:'Scegli un’immagine valida'},{status:400});if(file.size>8*1024*1024)return Response.json({error:'Dimensione massima 8 MB'},{status:400});const ext=file.name.split('.').pop()?.replace(/[^a-z0-9]/gi,'')||'jpg';const key=`covers/${crypto.randomUUID()}.${ext}`;await env.FILES.put(key,file.stream(),{httpMetadata:{contentType:file.type}});return Response.json({url:`/api/files/${key}`});}catch(error){return Response.json({error:error instanceof Error?error.message:'Errore caricamento'},{status:500});}}
